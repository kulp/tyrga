language: rust
rust:
  - stable
  - beta
  - nightly
before_script:
  - rustup component add clippy
  - cargo install cargo-with
script:
  - cargo clippy --tests
  - cargo build
  - cargo test
  - cargo doc
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
dist: xenial # for JDK
addons:
  apt:
    packages:
      - default-jdk-headless
